现在的视频转码工具，都是在本地运行的。我希望能够做一个视频转码的服务端。

## 基本信息

中文名：码上转
英文名：CloudCodec

提供 Web 界面，用户可以上传视频，选择好预设，或自定义转码配置，交给服务器转码。

转码配置的界面和参数参考 HandBrake 和小丸工具箱。

转码时，能看到实时的进度和日志。

转码完成后，用户可以下载转码后的视频。不管成功还是失败，都可以查看转码的日志。

可以批量转码：可以统一使用某种预设，也可以每个任务都使用不同的配置。

需要多用户管理。管理员可以查看所有用户的转码任务、日志。首个注册的用户即为管理员。

需要考虑 HTTP 访问，故ia登录注册时，对密码本身使用 SHA256 加密后，再传输。

## 本地文件访问与操作

管理员可以给用户开启本地文件访问的权限。

开启后，可以选取服务器上的视频文件，转码。

可以设置转码完成后的存放位置，命名规则。也可以设置原位替换被转码文件。

选取文件、设置路径时，应当使用类似于文件管理器的可视化界面。

## 管理员配置

管理员可以配置：

- 用户组及其权限
- 视频文件存放期限（针对用户上传的任务，针对用户组设置）
- 可选的预设列表和默认预设
- 视频文件大小限制（针对用户上传的任务，针对用户组设置）
- 本地文件访问权限（针对用户组设置）
- 自动化 API 访问权限（针对用户组设置）
- 本地文件访问的路径限制（针对用户组设置）
- 邮件通知设置

## 用户配置

用户可以配置：

- 可选预设和默认预设
- 转码完成后默认存放路径（需本地文件访问权限）
- 转码完成后默认命名规则（需本地文件访问权限）
- 个人信息和密码修改
- 邮件通知设置

## 编解码器

系统应当自动读取支持的编解码器和参数，供用户选择。

预设列表应当包含常用配置；需要工具编解码器支持情况，来自动调整预设。

需要支持尽可能全面的硬件编解码。

## 数据库

需要支持：

- SQLite
- MySQL
- PostgreSQL

默认使用 SQLite。

## 通知

集成通知功能，目前先支持邮件，后续需要更多的集成。

用户可以选择以下情况通知：

- 转码完成
- 转码失败

## 自动化 API

用户可以借此批量执行转码任务。需要管理员开启权限。

## 系统兼容性

需支持：

- Windows
- Linux
- macOS

其中，Linux 还需要特别支持群晖 DSM 系统（而且不是 Docker 容器，因为本地文件访问需要访问全部文件，这通过 Docker 容器的映射很麻烦。）

当然，也要有 Docker 容器支持。

架构上，要支持：

- amd64
- arm64